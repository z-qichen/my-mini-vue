# vuedraggable 拖拽排序不生效问题排查总结

## 问题现象

在问卷编辑器中，使用 `vuedraggable` 实现组件拖拽排序功能时：
- `dragstart` 和 `dragend` 事件正常触发，控制台能打印 `start` 和 `end`
- 但拖拽结束后，组件位置没有发生变化，仿佛排序未生效

## 涉及文件

- `src/views/EditorView/Center.vue` — 编辑器画布组件，包含 draggable 拖拽逻辑
- `src/stores/useEditor.ts` — 编辑器 Pinia 仓库，包含 `moveCom` 方法
- `src/stores/commands.ts` — 命令模式实现，包含 `createMoveCommand`

## 问题根因

### 双重修改导致抵消

`vuedraggable` 的 `v-model` 会**直接修改**绑定的数组。当拖拽发生时：

1. **vuedraggable 先修改数组**：把组件从旧位置移到新位置（例如 `[A,B,C]` → `[B,C,A]`）
2. **`dragend` 又执行一次 `moveCom`**：`createMoveCommand` 内部再次执行 `splice(oldIndex, 1)` 和 `splice(newIndex, 0, item)`

由于 `vuedraggable` 已经改变了数组顺序，`dragend` 里的 `oldIndex` 和 `newIndex` 是基于**修改后的数组**计算的，导致第二次 `splice` 操作的位置完全错乱，最终效果看起来就像"没变化"。

### 代码示意

```ts
// Center.vue 中（修改前）
<draggable v-model="store.coms" ...>

function dragend(event: { newIndex: number }) {
  // 此时 store.coms 已经被 vuedraggable 修改过了！
  store.moveCom(dragOldIndex, event.newIndex) // 基于错误的状态再次移动
}
```

```ts
// commands.ts 中
export function createMoveCommand(coms, oldIndex, newIndex) {
  return {
    execute: () => {
      const item = coms.splice(oldIndex, 1)[0]  // 第一次 splice
      coms.splice(newIndex, 0, item)            // 第二次 splice
    },
    ...
  }
}
```

## 解决方案

### 核心思路：隔离 vuedraggable 的直接修改

让 `vuedraggable` 操作一个**本地副本**，而不是直接操作 `store.coms`。拖拽结束后再通过 `moveCom` 统一修改仓库状态。

### 具体改动

#### 1. `v-model` 改为 `:list`

```vue
<!-- 修改前 -->
<draggable v-model="store.coms" ...>

<!-- 修改后 -->
<draggable :list="comsList" ...>
```

- `v-model`：双向绑定，vuedraggable 会直接修改原数组
- `:list`：单向绑定，vuedraggable 只操作传入的列表，不反向修改数据源

#### 2. 新增本地副本 `comsList`

```ts
// 本地列表副本，用于隔离 vuedraggable 的直接修改
const comsList = ref<Status[]>([])

watch(() => store.coms, (newVal) => {
  comsList.value = [...newVal]
}, { deep: true, immediate: true })
```

- `comsList` 是 `store.coms` 的浅拷贝
- `vuedraggable` 拖拽时只修改 `comsList`，不会触碰 `store.coms`
- 当 `store.coms` 发生变化（如新增、删除组件）时，`comsList` 自动同步

#### 3. `dragend` 逻辑保持不变

```ts
function dragend(event: { newIndex: number }) {
  if (dragOldIndex !== -1 && dragOldIndex !== event.newIndex) {
    store.moveCom(dragOldIndex, event.newIndex) // 现在只执行一次正确的移动
  }
  dragOldIndex = -1
}
```

由于 `store.coms` 未被 `vuedraggable` 污染，`moveCom` 里的 `splice` 操作基于正确的原始状态执行，排序结果正确。

## 流程对比

| 阶段 | 修改前（有问题） | 修改后（正常） |
|------|----------------|--------------|
| dragstart | 记录 oldIndex | 记录 oldIndex |
| 拖拽中 | `vuedraggable` 直接修改 `store.coms` | `vuedraggable` 修改本地 `comsList` |
| dragend | `moveCom` 基于被污染的数组再次 splice | `moveCom` 基于干净的 `store.coms` 正确 splice |
| 结果 | 移动两次，位置错乱或看似无变化 | 只移动一次，位置正确 |

## 额外收益

- **撤销/重做功能正常**：`moveCom` 通过 `createMoveCommand` 执行，天然支持历史记录和撤销操作
- **状态管理清晰**：仓库状态变更只能通过明确的 action（`moveCom`），避免外部组件直接修改

## 关键知识点

### vuedraggable 的 `v-model` vs `:list`

| 属性 | 行为 | 使用场景 |
|------|------|---------|
| `v-model` | 双向绑定，组件内部直接修改数组 | 简单场景，不需要额外逻辑 |
| `:list` | 单向传入，组件内部修改但不反向同步 | 需要手动控制状态变更的场景 |

### Pinia 状态修改原则

当使用第三方库直接操作 Pinia 状态时，要特别注意：
1. 第三方库可能会直接修改引用
2. 如果后续还有基于旧状态的操作，就会出现时序问题
3. 最佳实践：用本地副本隔离第三方库的修改，再通过明确的 action 同步到仓库
